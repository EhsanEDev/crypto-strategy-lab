# configs

Global (repository-level) configuration for future modules lives here.
Research owns its own configuration at `modules/research/config/`.

Never commit real secrets: API keys, private keys, exchange secrets,
Telegram bot tokens or Supabase service-role keys belong only in `.env`
(gitignored).
