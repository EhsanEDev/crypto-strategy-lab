# tests

Reserved for cross-module integration tests (future), e.g. Strategy
consuming Research outputs end-to-end.

Module tests live inside each module:

    modules/research/tests/    # run with: cd modules/research && pytest
